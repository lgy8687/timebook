# 双主线时间账本 — 设计文档

> 核心哲学：时间从不停止。每一分钟都在流，不存在"闲置"。  
> 所有操作都在回答同一个问题：**你的时间去哪了？**

---

## 一、双主线系统

### 主线 — 时间轴

| 属性 | 规则 |
|------|------|
| 核心 | 时间为主，活动为辅 |
| 时间 | **不可改，不可逆** |
| 状态 | 永不停止，永远有一个快捷按钮被按着（此消彼长） |

### 事件线 — 并行轴

| 属性 | 规则 |
|------|------|
| 核心 | 事件为主，时间为辅 |
| 时间 | 可以调（记错了时间段就改） |
| 状态 | 可随时启停，最终全部弹起来 |
| 层数 | 只设一级并行，不设二级三级嵌套 |
| 本质 | 从主线时间里"偷"出来的时间，告诉你"我还同时做了这件事" |

---

## 二、快捷入口（实时操作区）

### 主线快捷按钮
- **单选机制**：一次只能按一个，按此弹彼
- **永远有一个处于按下状态**（时间从不停止）
- 按 A → A 按下 → 再按 B → A 弹起，B 按下
- 记录的是"当前这整段时间在做什么"
- **视觉**：机械键盘键帽风格
  - 正方形（带微倒角），厚实立体感
  - 未按下（弹起）：键帽浮起，边缘高光 + 底部阴影
  - 已按下（选中）：键帽下沉，边缘阴影内收
  - 一眼看出哪个键被"按下去"了，像机械键盘的触感反馈

### 并行快捷按钮
- **独立开关**：每个按钮自启自停
- 按 A 开始 → 再按 A 结束（自始至终同一按钮负责）
- 可被其他并行按钮打断：A(开始) → B(打断A，A结束，B开始) → C(打断B，B结束，C开始) → C(结束)
- 一条并行事件的生命周期：**始于自己，终于自己**
- 最终所有并行按钮都会弹起

---

## 三、流水区手势操作

### 时间主线（非并行记录）

| 手势 | 操作 | 效果 |
|------|------|------|
| **右滑** | **删除** | 删掉的段 → 时间合并到**前一段** |
| **左滑** | **修改** | 换一级目录 / 二级目录 / 标签。**时间锁死，不可改** |
| **单击** | **补录并行** | 在当前时段上叠加一条并行事件 |
| **长按** | **切割** | 从当前段里取一个子区间，拆段 |

### 事件线（并行记录）

| 手势 | 操作 | 效果 |
|------|------|------|
| **右滑** | **删除** | 直接删，不做合并 |
| **左滑** | **修改** | 与补录用同一套 drawer 界面。调时间区间 / 换目录都行。**与补录功能无区别** |

并行记录没有单击和长按——因为它的功能已经完整。

---

## 四、删除规则

### 主线删除 → 合并到前段

```
上班 13-15 | 摸鱼 15-16 | 上班 16-18
```
      ↓ 且"摸鱼"时段内有一条并行记录（"听歌"）
删除"摸鱼"15-16 →

```
上班 13-16 | 上班 16-18
```
并行记录"听歌"一并删除，不合并到下一段

**规则：**
- 时间只能往前合并。因为时间流的方向是从过去→未来，空档归前一段管
- **如果被删除的时间段内包含并行事件，并行事件一并删除**。不存在并行事件合并到下一段场景

### 并行删除 → 直接删

不做合并。

---

## 五、切割规则

切割一定是"取一段"，不能"改整段"（改整段是修改的事）。

### 切割的结果取决于取的位置

```
上班 9-12
```

**取中间**（10-10:30）→ 改成"摸鱼"：
```
上班 9-10 | 摸鱼 10-10:30 | 上班 10:30-12
```
→ 三段独立，无需合并

**取前段**（9-9:30）→ 改成"开会"：
```
开会 9-9:30 | 上班 9:30-12
```
→ 两段

**取全部** → 跟修改一样？ → 不对，切割不能取全部，取全部是修改的事

### 关键规则
- 切出来的段跟前后活动 **一样** → 自动合并
- 切出来的段跟前后活动 **不一样** → 独立存在
- 切割不强行合并。删除了才合并

---

## 六、修改规则

### 主线修改
- **可改：** 一级目录、二级目录、标签（但标签不能脱离目录存在）
- **不可改：** 开始时间、结束时间、时长
- 时间锁死，只换标签

### 并行修改
- **可改：** 时间区间（记错了就重选）、目录
- 与补录是同一套界面，功能相同
- 本质是"时间选错了，重新定位到正确的时间段上"

---

## 七、界面统一

### drawer 操作界面（统一模板）

补录、并行补录、切割都用同一个 drawer：

```
┌─────────────────────┐
│  选择时间段          │
│  ┌─┐ ┌─┐ ┌─┐      │
│  │9│:│0│:│0│       │
│  └─┘ └─┘ └─┘      │
│  ── 至 ──           │
│  ┌──┐ ┌──┐ ┌──┐   │
│  │12│:│0│:│0│      │
│  └──┘ └──┘ └──┘   │
├─────────────────────┤
│  选择分类            │
│  [一级目录] → [二级] │
├─────────────────────┤
│  备注（选填）        │
│  ┌─────────────────┐│
│  │                 ││
│  └─────────────────┘│
├─────────────────────┤
│      [确定]          │
└─────────────────────┘
```

差异仅在：
- **补录：** 时间段可选范围是整个空白段
- **切割：** 时间段锁定在当前活动时长内，不可超出
- **并行修改：** 时间段可自由选（因为时间只是辅助信息）

### 修改弹窗（仅主线，左滑触发）

```
┌─────────────────────┐
│  修改记录            │
│  上班 / 搬砖 — 180min│
├─────────────────────┤
│  开始时间：09:00     │ ← 只显示，不可改
│  时长：180分钟       │ ← 只显示，不可改
├─────────────────────┤
│  [选择分类 →] 上班   │ ← 可改
│  备注：[________]    │ ← 可改
├─────────────────────┤
│  [取消]    [确定]    │
└─────────────────────┘
```

---

## 八、操作逻辑对照总表

| 操作 | 所属线 | 修改时间？ | 修改标签？ | 手势 |
|------|--------|-----------|-----------|------|
| 修改 | 主线 | ❌ | ✅ | 左滑 |
| 切割 | 主线 | ❌（取区间） | ✅（只改取的段） | 长按 |
| 删除 | 主线 | — | — | 右滑，合并前段 |
| 补录并行 | 事件线 | ✅ | ✅ | 单击 |
| 并行修改 | 事件线 | ✅ | ✅ | 左滑 |
| 并行删除 | 事件线 | — | — | 右滑，直接删 |
