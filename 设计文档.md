# TimeBook 设计文档

> 核心哲学：**时间从不停止**。每一秒都在消耗，不存在"闲置"。  
> 如果用户不主动记录，上一段活动自动延续——软件永远不会显示"未使用的时间"。
>
> 所有操作都在回答同一个问题：**你的时间去哪了？**

---

## 一、双主线系统

系统由两条并行的"线"构成，互不干扰但相互关联。

### 1.1 主线 — 时间轴

| 属性 | 规则 |
|------|------|
| 核心 | 时间为主，活动为辅 |
| 时间 | **不可改，不可逆** |
| 状态 | 永不停止，永远有一个快捷按钮被按着（此消彼长） |

- 时间轴上每时每刻都被某个活动占据
- 快捷按钮是**单选机制**：按 A → A 按下 → 再按 B → A 弹起，B 按下
- 流水的修改弹窗中，开始时间和时长**只显示不可编辑**

### 1.2 事件线 — 并行轴

| 属性 | 规则 |
|------|------|
| 核心 | 事件为主，时间为辅 |
| 时间 | 可以调（记错了时间段就改） |
| 状态 | 可随时启停，最终全部弹起来 |
| 层数 | 只设一级并行，不设二级三级嵌套 |
| 本质 | 从主线时间里"偷"出来的时间，告诉你"我还同时做了这件事" |

- 并行按钮是**独立开关**：每个按钮自启自停
- 按 A 开始 → 再按 A 结束（自始至终同一按钮负责）
- 可被其他并行按钮打断：A(开始) → B(打断A，A结束，B开始) → C(打断B，B结束，C开始) → C(结束)
- 一条并行事件的生命周期：**始于自己，终于自己**
- 最终所有并行按钮都会弹起

---

## 二、数据模型

所有数据用 localStorage 持久化，key 前缀 `v9_`：

### 2.1 分类目录 `v9_cats`

```json
[
  { "id": 1, "name": "上班", "icon": "💼", "color": "#3b82f6",
    "subs": ["开网约车", "开会", "写代码", "办公"] },
  ...
]
```

- 一级分类 = 大类（上班、生活、交通、餐饮等）
- 二级分类 = 具体活动（subs 数组）
- 每个一级分类有自己的颜色和图标

### 2.2 快捷入口 `v9_shorts`

```json
[
  { "l1": "上班", "l2": "开网约车", "icon": "🚕" },
  ...
]
```

- 上限 9 个
- 显示在首页顶部，机械键盘键帽样式
- 长按可修改 Emoji

### 2.3 并行快捷 `v9_parallel_shorts`

同 `v9_shorts` 格式，显示在并行区，上限 9 个

### 2.4 流水日志 `v9_logs`

```json
[
  {
    "id": 1747651200000.123,
    "startTime": 1747651200000,  // 毫秒时间戳（北京时间）
    "endTime": 1747654800000,
    "duration": 60,               // 分钟
    "l1": "上班",
    "l2": "开网约车",
    "tag": "",                     // 标签
    "note": "",                    // 备注
    "color": "#3b82f6",
    "parallel": false,             // 是否为并行事件
    "parentId": null               // 并行事件归属的主线条目 ID
  },
  ...
]
```

- 按 startTime 倒序排列
- parallel=false 为主线条目，parallel=true 为并行事件
- parentId 将并行事件关联到所属的主线时间段

### 2.5 当前状态

- `v9_current`：当前正在记录的主线活动 `{ id, startTime, l1, l2, tag, note, color }`
- `v9_parallel`：当前正在运行的并行活动 `{ id, startTime, l1, l2, icon, note }`
- `v9_parallel_history`：在当前主线时段内已结束的并行事件暂存区

---

## 三、页面结构

共三个 Tab 页面，底部导航切换：

### 3.1 流水页（首页）

**顶部区域：**
- 状态灯 + 当前活动名称 + 计时器
- 版本号（红字，用于 cache-busting）

**今日剩余进度条：**
- 深色仿皮革纹理背景 + 金色填充条
- 🔥 火焰动效跟随进度移动
- 显示今天已用百分比

**快捷入口区（主页核心）：**
- 主线快捷按钮（灰色键帽，按此弹彼）
- 并行快捷按钮（紫色按钮，独立开关）
- "+"按钮 → 弹出分类选取器（drawer）
- 自由输入框 `#标签 内容` + 时间回溯区间

**时钟面板：**
- 2 个圆形时钟并排：60分钟环 + 24小时环
- 60分钟环：显示当前活动在本小时内的占比（倒计时）
- 24小时环：显示今天各活动的时段分布（色块）+ 还剩多少时间

**流水列表（日志）：**
- 今天日志不折叠，过去日志截断 10 条，可"加载更多"
- 每条日志以日期分组，每天有日期头
- 实时卡片（当前活动）始终置顶，显示实时计时
- 并行区段：缩进在主线条目下方

### 3.2 报表页

- 日期展示（北京时间）
- 当前活动状态
- 一级分类/全部 切换按钮
- 环形图（donut chart）：时间占比可视化
- 24 小时时间轴条：彩色条形显示全天活动分布
- 一级目录分布：横向柱状图
- 活动明细：二级分类柱状图
- 详细记录列表

### 3.3 设置页

- 首页大图标设置（图标/隐藏切换 + 添加/删除快捷入口）
- 并行快捷设置
- 全量目录管理（添加/编辑/删除一级分类和二级分类、修改颜色、修改图标）
- Factory Reset 按钮

---

## 四、手势操作

流水卡片支持以下手势：

### 4.1 主线条目（非并行记录）

| 手势 | 操作 | 效果 |
|------|------|------|
| **右滑** | **删除** | 删掉的段 → 时间合并到**前一段**（同一天内）。被删时段内的并行事件一并删除 |
| **左滑** | **修改** | 换一级目录 / 二级目录 / 标签。**时间锁死，不可改** |
| **单击** | **补录并行** | 在当前时段上叠加一条并行事件，通过 drawer 选时间段 |
| **长按** | **切割** | 从当前段里取一个子区间，拆成独立的新活动段 |

### 4.2 并行记录

| 手势 | 操作 | 效果 |
|------|------|------|
| **右滑** | **关闭/删除** | 运行中的并行→关闭（不记入日志）；已结束的并行→直接删 |
| **左滑** | **切换/修改** | 运行中的并行→切换活动；已结束的并行→修改目录 |

并行记录没有单击和长按——功能已完整。

---

## 五、删除规则

### 5.1 主线删除 → 合并到前段

```
上班 13-15 | 摸鱼 15-16 | 上班 16-18
```

删除"摸鱼"15-16 →

```
上班 13-16 | 上班 16-18
```

**规则：**
- 时间只能往前合并（过去→未来，空档归前一段管）
- 如果被删除的时间段内包含并行事件，并行事件**一并删除**
- 删除后相邻同活动自动合并

### 5.2 并行删除 → 直接删

不做合并。

---

## 六、切割规则

切割一定是"取一段"，不能"改整段"（改整段是修改的事）。

### 6.1 切割结果取取决于取的位置

```
上班 9-12
```

**取中间**（10-10:30）→ 改成"摸鱼"：
```
上班 9-10 | 摸鱼 10-10:30 | 上班 10:30-12
```
→ 三段独立，无需合并

**取前段**（9-9:30）→ 改成"开会"：
```
开会 9-9:30 | 上班 9:30-12
```
→ 两段

**取全部** → 不是切割该做的事（修改来做）

### 6.2 关键规则
- 切出来的段跟前后活动**一样** → 自动合并
- 切出来的段跟前后活动**不一样** → 独立存在
- 切割不强行合并。删除了才合并
- 切割区间不能与已有并行事件冲突

---

## 七、修改规则

### 7.1 主线修改

- **可改：** 一级目录、二级目录、备注
- **不可改：** 开始时间、结束时间、时长
- 时间锁死，只换标签

### 7.2 并行修改

- **可改：** 时间区间（记错了就重选）、目录、备注
- 与补录是同一套界面，功能相同
- 本质是"时间选错了，重新定位到正确的时间段上"

---

## 八、操作逻辑对照总表

| 操作 | 所属线 | 修改时间？ | 修改标签？ | 手势 |
|------|--------|-----------|-----------|------|
| 修改 | 主线 | ❌ | ✅ | 左滑 |
| 切割 | 主线 | ❌（取区间） | ✅（只改取的段） | 长按 |
| 删除 | 主线 | — | — | 右滑，合并前段 |
| 补录并行 | 事件线 | ✅ | ✅ | 单击 |
| 并行修改/切换 | 事件线 | ✅ | ✅ | 左滑 |
| 并行删除/关闭 | 事件线 | — | — | 右滑，直接删 |

---

## 九、Drawer 操作界面（通用模板）

补录、并行补录、切割都用同一个 drawer：

```
┌─────────────────────┐
│  时间段选择            │
│  HH:MM:SS  →  HH:MM:SS │
│  [───进度条(可拖拽)───] │
│  开始时间  结束时间     │
├─────────────────────┤
│  选择分类              │
│  一级目录 → 二级目录    │
├─────────────────────┤
│  备注（选填）          │
└─────────────────────┘
```

差异仅在：
- **补录：** 时间段可选范围是整个空白段
- **切割：** 时间段锁定在当前活动时长内，不可超出
- **并行修改：** 时间段可自由选（因为时间只是辅助信息）

### 进度条拖拽

- 两个手柄（start / end）可独立拖动
- 磁吸机制：落在已有并行区段时自动吸附到最近空闲边界
- 点击进度条空白区域：最近的手柄跳转至该位置
- 灰色块显示该时段内已有的并行占用

### 修改弹窗（仅主线，左滑触发）

```
┌─────────────────────┐
│  修改记录              │
│  上班 / 搬砖 — 3:00:00│
├─────────────────────┤
│  开始时间：09:00:00   │ ← 只显示，不可改
│  时长：3:00:00        │ ← 只显示，不可改
├─────────────────────┤
│  [选择分类 →] 上班    │ ← 可改
│  备注：[________]     │ ← 可改
├─────────────────────┤
│  [取消]    [确定]      │
└─────────────────────┘
```

---

## 十、自由输入

- 格式：`#标签 内容`
- 支持回溯补录：展开时间选择器，填开始/结束时间
- 适用于不想在目录里找活动、快速记一笔的场景

---

## 十一、并行结转

当主线切换到新活动时，如果并行还在运行：

- **一起结束**：主线切换时，当前并行也结束并记入日志
- **结转**：当前并行结算、记入日志后，**立即重启新的并行时段**（ID 更新，startTime 重置为当前时间），延续到新的主线时段

结转逻辑通过确认弹窗让用户选择。

---

## 十二、时钟可视化

### 60 分钟环
- 显示当前活动在本小时内的消耗占比
- 倒计时秒针实时转动
- 中间显示剩余分钟数

### 24 小时环
- 显示当天各活动的时段分布（彩色弧段）
- 带小时刻度（每 3 小时粗刻度）
- 实时秒针
- 中间显示还剩多少时间

### 环的设计原则
- 环保持干净，只显示色块和刻度
- 颜色与活动名称的对应关系通过环下方的图例/引用表展示
- 不在环上画放射线或拉文字
- 环是"表盘"，图例是"说明书"

---

## 十三、键帽按钮风格

### 主线快捷按钮
- 机械键盘键帽风格
- 正方形（带微倒角），厚实立体感
- **弹起状态：** 键帽浮起，底部阴影 + 边缘高光
- **按下状态（选中）：** 键帽下沉，阴影内收，背景微变深
- 视觉上可一眼看出哪个键被"按下去"了

### 并行按钮
- 同款键帽风格（紫色系）
- 独立开关，按一次开始，再按一次结束

---

## 十四、时间与地区

- 时区：**北京时间 UTC+8**（所有时间显示、计算均基于北京时区）
- 时间精度：精确到**秒**
- 日切换点：北京时间 00:00（24:00）
- 计时器使用 `requestAnimationFrame` 驱动，每秒更新一次 UI

---

## 十五、版本规范

### 版本格式

版本号采用 **两段式**：`v4.x`

- `4` = 主版本号（当前主版本）
- `x` = **上传/更新次数**，每上线一次递增 1
- 数字越小 → 改动越少；数字越大 → 上传次数越多，改动积累越多
- 当前版本：`v4.6`（已上线 6 次）

### 更新规则

- 每次上线必须更新 `index.html` 中所有版本标签（cache-buster + page-version-badge）
- 部署方式：GitHub Pages（`main` 分支根目录）
- 缓存控制：版本号后接 `?v=4.x` 参数 + HTTP Cache-Control 头

---

## 十六、未来展望

### 当前阶段：单人 Beta

目前 TimeBook 所有数据存在浏览器 localStorage 中，纯本地运行，只适用于单人使用。部署通过 GitHub Pages，任何有链接的人都可以打开，但数据互不相通。

这是验证产品形态的阶段——先把体验做对，功能做全。

### 下一阶段：多用户 + 云端

**目标：** 从个人时间账本升级为**公共时间管理平台**。

关键变化：

| 维度 | 当前 | 未来 |
|------|------|------|
| 数据存储 | localStorage（浏览器本地） | 云端数据库（云端） |
| 用户 | 单人 | 多用户，账号系统 |
| 数据互通 | 不互通 | 共享/对比/排行榜 |
| 后台 | 无 | 管理后台 |

**规划方向：**

- **用户注册/登录** — 每个人有自己的数据空间
- **云端同步** — 手机、电脑、平板数据实时同步
- **数据看板** — 个人统计、周报月报自动化
- **社交功能** — 时间分配对比、团队时间管理、排行榜
- **API 接口** — 允许第三方工具接入（日历、待办清单等）
- **公共模板** — 用户分享自己的活动分类体系

### 长期愿景

> 让时间管理从一个"个人笔记工具"变成**记录每一秒去向的基础设施**。
>
> 从「我的时间去哪了」到「我们大家的时间去哪了」。

| 术语 | 含义 |
|------|------|
| 主线 | 时间轴上的活动序列，不可有空档 |
| 并行/事件线 | 从主线时间里"偷"出来的重叠活动 |
| 结转 | 主线切换时，并行结算后自动重启 |
| 补录 | 在已结束的主线时段里追加并行记录 |
| 切割 | 把一段主线活动切成多段，中间换成别的活动 |
| 流水 | 时间轴上的日志列表 |
| 键帽/按下 | 机械键盘按钮风格的按下/弹起状态 |
| 磁吸 | 进度条拖拽时自动吸附到空闲区间边界 |
| 回溯 | 给过去的某个时间段补填记录 |
